import 'package:flutter/material.dart';

class Start2dCharacter extends StatelessWidget {
  const Start2dCharacter({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("仮ページ"),
      )
    );
  }
}
